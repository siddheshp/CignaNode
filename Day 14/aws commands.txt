Add IAM user policy :  AmazonEC2ContainerRegistryPowerUser

797721580785.dkr.ecr.us-east-1.amazonaws.com/ems-backend

AWS_REGION = 'us-east-1'  
AWS_ACCOUNT_ID = '797721580785'
ECR_REPOSITORY = 'ems-backend'
IMAGE_TAG = "v1"
ECR_REGISTRY = "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
IMAGE_NAME = "${ECR_REGISTRY}/${ECR_REPOSITORY}"

aws ecr get-login-password --region %AWS_REGION% | podman login --username AWS --password-stdin %ECR_REGISTRY%

podman push ${IMAGE_NAME}:${IMAGE_TAG}
podman push ${IMAGE_NAME}:latest